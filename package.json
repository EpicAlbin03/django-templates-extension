{
	"name": "django-templates-extension",
	"version": "1.0.0",
	"author": "django-templates-extension contributors",
	"license": "MIT",
	"private": true,
	"scripts": {
		"bootstrap": "pnpm build",
		"build": "tsc -b",
		"test": "cross-env CI=true pnpm --filter django-vscode test",
		"watch": "tsc -b -watch",
		"format": "prettier --write .",
		"lint": "prettier --check ."
	},
	"dependencies": {
		"typescript": "^6.0.3"
	},
	"devDependencies": {
		"cross-env": "^7.0.2",
		"prettier": "~3.3.3"
	},
	"packageManager": "pnpm@9.3.0"
}
